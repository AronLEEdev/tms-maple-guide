# Front Matter 欄位規範

## 1. 適用範圍

本規範適用於 `內容/` 與 `來源/` 下的正式 Markdown 頁面。`範本/`、專案說明及維護文件不納入內容驗證。

## 2. 共用必填欄位

| 欄位 | 格式 | 說明 |
|---|---|---|
| `id` | 小寫英文、數字、連字號 | 不因檔名或標題改變的穩定 ID |
| `title` | 文字 | 頁面標題，使用繁體中文或來源原文 |
| `type` | 枚舉 | `event`、`guide`、`version`、`system`、`glossary`、`source`、`test-record` |
| `server` | 枚舉 | `TMS`、`KMS`、`GMS`、`JMS`、`CMS`、`MSEA`、`MULTI` |
| `status` | 枚舉 | `draft`、`needs-verification`、`verified`、`outdated`、`archived` |
| `created_at` | `YYYY-MM-DD` | 首次建立日期 |
| `updated_at` | `YYYY-MM-DD` | 最後內容更新日期 |
| `tags` | 文字陣列 | 搜尋與分類標籤，可為空陣列 |

## 3. 共用選填欄位

| 欄位 | 格式 | 說明 |
|---|---|---|
| `version` | 文字 | 適用版本；不確定時不得猜測 |
| `verified_at` | `YYYY-MM-DD` 或 `null` | 最後完成 TMS 驗證的日期 |
| `source_ids` | ID 陣列 | 支援本頁結論的來源 ID |
| `related_ids` | ID 陣列 | 相關內容的穩定 ID |
| `aliases` | 文字陣列 | 常見舊稱、英文名或縮寫 |
| `summary` | 文字 | 可供索引與搜尋顯示的一句話摘要 |

`status: verified` 時必須提供 `verified_at`。TMS 正式活動、攻略與系統頁面原則上至少需要一筆 `source_ids`。

## 4. 活動欄位

| 欄位 | 必填 | 說明 |
|---|---:|---|
| `starts_at` | 是 | 活動開始時間，必須包含 `+08:00` |
| `ends_at` | 是 | 活動結束時間，必須包含 `+08:00` |
| `claim_deadline` | 否 | 獎勵領取期限；無獨立期限時使用 `null` |
| `reset` | 否 | `daily`、`weekly`、`none`、`special` |
| `participation_scope` | 否 | `account`、`world`、`character`、`mixed` |
| `audiences` | 否 | 新手、回谷玩家、特定等級等目標玩家 |
| `completion_mode` | 是 | `cumulative`、`staged`、`independent`、`mixed` |
| `completion_requirements` | 是 | 玩家必須完成的任務、次數、點數或其他門檻 |
| `reward_tiers` | 是 | 獎勵與其對應的達成條件 |

活動狀態目錄與 `status` 是不同概念。活動結束後應移至 `已歸檔/`，並將內容狀態改為 `archived`。

### 4.1 達成條件

`completion_requirements` 的每一筆條件必須具有穩定的頁內 ID，並記錄：

| 子欄位 | 說明 |
|---|---|
| `id` | 條件的頁內 ID，例如 `daily-check-in` |
| `name` | 玩家看得懂的條件名稱 |
| `category` | `login`、`quest`、`hunt`、`boss`、`collection`、`currency`、`other` |
| `period` | `once`、`daily`、`weekly`、`event` |
| `scope` | `account`、`world`、`character` |
| `target` | 需要達成的數量；無法量化時可為 `null` |
| `unit` | 次數、隻、點數、枚等單位；無數量時可為 `null` |
| `required` | 是否為取得核心獎勵的必要條件 |

複雜前置、可替代條件、組隊限制和例外情況仍應在正文說明，不應只依賴 YAML。

### 4.2 獎勵與門檻

`reward_tiers` 用來建立獎勵與達成條件的明確關係：

| 子欄位 | 說明 |
|---|---|
| `id` | 獎勵階段的頁內 ID |
| `name` | 獎勵或獎勵階段名稱 |
| `requirement_ids` | 需要達成的 `completion_requirements` ID |
| `rewards` | 此階段實際發放的獎勵名稱陣列 |
| `choice_type` | `all`、`select`、`random` |
| `repeatable` | 是否能重複取得 |

YAML 負責讓網站和 AI 知道「哪個門檻對應哪一組獎勵」；正文則必須提供玩家可閱讀的條件表、獎勵表、完成天數計算、容錯與推薦路線。正式發布的活動不得只有活動時間而缺少達成條件和獎勵對照。

## 5. 攻略欄位

| 欄位 | 必填 | 說明 |
|---|---:|---|
| `guide_kind` | 是 | `getting-started`、`returning`、`leveling`、`quest`、`progression`、`boss`、`class`、`system` |
| `audiences` | 是 | 目標玩家陣列 |
| `level_min` | 否 | 最低適用等級 |
| `level_max` | 否 | 最高適用等級 |
| `investment` | 否 | `free`、`low`、`medium`、`high`、`any` |
| `prerequisite_ids` | 否 | 前置內容或系統 ID |

## 6. 版本欄位

版本頁面必須提供 `version`、`released_at` 及 `source_ids`。`released_at` 使用 `YYYY-MM-DD`。

## 7. 來源欄位

| 欄位 | 必填 | 說明 |
|---|---:|---|
| `publisher` | 是 | 官方單位、網站、作者或社群名稱 |
| `source_tier` | 是 | `official`、`community`、`other-server` |
| `url` | 是 | 原始頁面網址，不使用搜尋結果網址 |
| `published_at` | 否 | 原始發布日期 |
| `source_updated_at` | 否 | 原始來源最後更新日期 |
| `retrieved_at` | 是 | 本專案擷取或閱讀日期 |
| `access_status` | 是 | `available`、`changed`、`unavailable` |
| `archived_url` | 否 | 合法公開封存網址 |
| `snapshot_path` | 否 | 獲准保存的本機快照路徑 |
| `content_hash` | 否 | 來源內容雜湊 |
| `supports` | 是 | 此來源能支援的具體結論陣列 |

## 8. 實測紀錄欄位

實測紀錄必須描述測試日期、TMS 版本、測試條件、操作方式及結果。角色名稱不是必填，公開前不得包含帳號、登入資訊或其他私人資料。

## 9. 日期與狀態規則

- 日期一律使用西元絕對日期。
- 活動時間一律包含台灣時區 `+08:00`。
- `updated_at` 不得早於 `created_at`。
- `verified_at` 不得晚於 `updated_at`，除非只是重新驗證且同步更新 `updated_at`。
- 內容有疑問時使用 `needs-verification`，不得維持 `verified`。
- 舊內容原則上標記過期或歸檔，不直接刪除。
